namespace PenguinRun
{
    internal enum PowerUpKind
    {
        // ── 通用道具 ──────────────────────────────────────────
        Dash,               // 鱼干冲刺：短暂加速
        Magnet,             // 极光磁针：吸引金币
        Shield,             // 团团护盾：一次无敌
        ScoreStar,          // 星光加分：分数倍率
        GlideFeather,       // 极地滑翔：滑翔延长
        DoubleFishSnack,    // 双倍鱼干：金币翻倍
        TimeHourglass,      // 时间沙漏：慢动作
        FishBomb,           // 鱼形炸弹：受伤时自动抵消一次
        SecondHeart,        // 额外生命：上限+1

        // ── 海洋珊瑚礁专属 ────────────────────────────────────
        BubbleShield,       // 泡泡护盾：强化护盾，可水中冲刺
        SeahorseBoost,      // 海马加速：爆发速度

        // ── 天空飞翔专属 ──────────────────────────────────────
        CloudWalk,          // 踏云而行：空中二段跳
        WindRider,          // 乘风滑翔：延长滑翔

        // ── 冰湖回音谷专属 ────────────────────────────────────
        IceMirror,          // 冰镜反射：短时间碰撞障碍被弹开而不受伤

        // ── 极光磁场专属 ──────────────────────────────────────
        AuroraChain,        // 极光连锁：金币连击窗口大幅延长，连击不断

        // ── 北境雾堤专属 ──────────────────────────────────────
        FogLantern,         // 雾灯：在雾堤段提前照亮障碍（同时激活长时磁吸+慢动作）

        // ── 雪松废墟专属 ──────────────────────────────────────
        TreantArmor,        // 树人护甲：减速但连续吸收 2 次伤害

        // ── 海洋通用强化 ──────────────────────────────────────
        CoralBounce,        // 珊瑚回弹：护盾强化，下次被击时弹出并触发短暂无敌冲刺

        // ── 天空通用强化 ──────────────────────────────────────
        ThunderFeather,     // 雷羽：冲刺+磁吸复合，冲刺期间可摧毁小障碍
    }
}
